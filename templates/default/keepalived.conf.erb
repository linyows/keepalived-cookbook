! Configuration File for keepalived
! Generated by Chef.

global_defs {
  notification_email {
    <% Array(@notification_emails).each do |email| %>
    <%= email %>
    <% end -%>
  }
  notification_email_from <%= @notification_email_from %>
  smtp_server <%= @smtp_server %>
  smtp_connect_timeout <%= @smtp_connect_timeout %>
  router_id <%= @router_id %>
}

<% Array(@include_files).each do |file| %>
include /etc/keepalived/conf.d/<%= file %>
<% end %>
